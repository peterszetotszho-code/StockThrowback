# Optional dependencies for the RAG / agent / observability layers.
# Install with: pip install -r requirements-optional.txt

# Vector store + hybrid retrieval + reranking
chromadb>=0.5.0
rank-bm25>=0.2.2
sentence-transformers>=3.0.0

# LLM (report generation and agent reasoning)
openai>=1.40.0
